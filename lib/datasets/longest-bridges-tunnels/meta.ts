import type { DatasetMeta } from "../types";

const meta: DatasetMeta = {
  slug: "longest-bridges-tunnels",
  title: "Longest Bridges & Tunnels in the World (CSV)",
  description:
    "Browse the world's longest bridges and tunnels, including location, length in kilometres, year completed, what they carry, and notable facts. Free CSV preview — sort, filter, and inspect in your browser.",
  keywords: [
    "longest bridges csv",
    "longest tunnels dataset",
    "world bridges tunnels list",
    "bridge length data",
    "tunnel length by country",
    "infrastructure records",
  ],
  firstRowAsHeader: true,
};

export default meta;
