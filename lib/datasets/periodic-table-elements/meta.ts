import type { DatasetMeta } from "../types";

const meta: DatasetMeta = {
  slug: "periodic-table-elements",
  title: "Periodic Table Elements (CSV)",
  description:
    "Browse all elements of the periodic table with atomic number, symbol, period, group, block, atomic mass, state at STP, category, year discovered, discoverer, and key uses. Free CSV preview — sort, filter, and inspect in your browser.",
  keywords: [
    "periodic table csv",
    "chemical elements dataset",
    "elements atomic number",
    "periodic table data",
    "element atomic mass",
    "chemistry elements list",
  ],
  firstRowAsHeader: true,
};

export default meta;
