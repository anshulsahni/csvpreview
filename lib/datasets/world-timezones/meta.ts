import type { DatasetMeta } from "../types";

const meta: DatasetMeta = {
  slug: "world-timezones",
  title: "World Timezones (CSV)",
  description:
    "Browse all major world timezones, including UTC offsets, DST offsets, regions, major cities, and countries. Covers UTC-12 to UTC+14. Free CSV preview — sort, filter, and inspect in your browser.",
  keywords: [
    "world timezones csv",
    "UTC offset dataset",
    "timezone data",
    "DST offset",
    "timezones by region",
    "timezone list",
  ],
  firstRowAsHeader: true,
};

export default meta;
